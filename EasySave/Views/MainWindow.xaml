<Window x:Class="EasySave.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySave"
        xmlns:vm="clr-namespace:EasySave.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <TabControl>
        <TabItem Header="Create Jobs">
            <Grid>
                <Label Content="Jobs creation" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="18" FontWeight="Bold"/>


                <StackPanel Margin="40,80,500,0" Grid.ColumnSpan="3">
                    <Label Content="Name" HorizontalAlignment="Left" FontSize="14" Margin="10,0" Height="30"></Label>
                    <TextBox x:Name="textBoxJobName" Margin="10,0,0,5" VerticalAlignment="Center"/>

                    <Label Content="Source" HorizontalAlignment="Left" FontSize="14" Margin="10,0" Height="30"></Label>
                    <DockPanel Margin="10,0,0,5" VerticalAlignment="Center">
                        <TextBox x:Name="textBoxJobSource" IsReadOnly="True" DockPanel.Dock="Left" Width="200" Margin="0,0,3,0"/>
                        <Button Content="" Click="BrowseSource_Click" DockPanel.Dock="Right"/>
                    </DockPanel>

                    <Label Content="Target" HorizontalAlignment="Left" FontSize="14" Margin="10,0" Height="30"></Label>
                    <DockPanel Margin="10,0,0,5" VerticalAlignment="Center">
                        <TextBox x:Name="textBoxJobTarget" IsReadOnly="True" DockPanel.Dock="Left" Width="200" Margin="0,0,3,0"/>
                        <Button Content="" Click="BrowseTarget_Click" DockPanel.Dock="Right"/>
                    </DockPanel>

                    <Label Content="Type" HorizontalAlignment="Left" FontSize="14" Margin="10,0" Height="30"></Label>
                    <ComboBox x:Name="textBoxJobType" Margin="10, 0"  VerticalAlignment="Center">
                        <ComboBoxItem IsSelected="True" Content="Full"/>
                        <ComboBoxItem Content="Diff"/>
                    </ComboBox>

                    <Button Content="Create" Click="btnCreateJob" Width="50" Height="30" Margin="0,30,0,0"/>
                </StackPanel>


                <DataGrid Name="dgLstFile1" AutoGenerateColumns="False" Grid.Row="0" Margin="0,100,30,50" HorizontalAlignment="Right" Width="400" Grid.Column="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding JobName}"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding PathSource}"/>
                        <DataGridTextColumn Header="Target" Binding="{Binding PathTarget}"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                    </DataGrid.Columns>
                </DataGrid>


            </Grid>
        </TabItem>
        
        
        <TabItem Header="Execute Jobs">
            <Grid>
                <Label Content="Execute Jobs" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="18" FontWeight="Bold"/>

                <DataGrid Name="dgLstFile2" AutoGenerateColumns="False" Grid.Row="0" Margin="0,80,0,200" HorizontalAlignment="Center" Width="700">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding JobName}"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding PathSource}"/>
                        <DataGridTextColumn Header="Target" Binding="{Binding PathTarget}"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <DockPanel VerticalAlignment="Center" Margin="0,150,500,0" Width="200" Height="25">
                    <Label Content="name :"  DockPanel.Dock="Left"/>
                    <TextBox x:Name="textBoxJobExecute" DockPanel.Dock="Right"/>
                </DockPanel>

                <Button Content="Execute" Click="btnExecuteJob" Width="50" Height="30" Margin="0,250,500,0"/>

                
                <Border BorderBrush="Black" BorderThickness="1" Width="400" Height="150" Margin="270,210,0,0">
                    <ScrollViewer>
                        <StackPanel x:Name="stackPanel">

                        </StackPanel>
                    </ScrollViewer>
                </Border>



            </Grid>
        </TabItem>
        
        
        <TabItem Header="Delete Jobs">
            <Grid>
                <Label Content="Delete Jobs" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="18" FontWeight="Bold"/>

                <DockPanel VerticalAlignment="Center" Margin="0,0,450,0" Width="200" Height="25">
                    <Label Content="name :"  DockPanel.Dock="Left"/>
                    <TextBox x:Name="textBoxJobDelete" DockPanel.Dock="Right"/>
                </DockPanel>

                <Button Content="Delete" Click="btnDeleteJob" Width="50" Height="30" Margin="0,200,450,0"/>
                
                
                <DataGrid Name="dgLstFile3" AutoGenerateColumns="False" Grid.Row="0" Margin="0,100,30,50" HorizontalAlignment="Right" Width="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding JobName}"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding PathSource}"/>
                        <DataGridTextColumn Header="Target" Binding="{Binding PathTarget}"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                    </DataGrid.Columns>
                </DataGrid>

                
            </Grid>
        </TabItem>
        
        
        <TabItem Header="Settings">
            <Grid>
                <Label Content="Settings" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="18" FontWeight="Bold"/>

                <DockPanel VerticalAlignment="Center" Margin="0,0,500,0" Width="130" Height="25">
                    <Label Content="extension :"  DockPanel.Dock="Left"/>
                    <ComboBox ItemsSource="{Binding Options}" SelectedValue="{Binding SelectedValue}" DisplayMemberPath="Name" Width="50" Height="20" Margin="0,0,500,0"/>
                </DockPanel>
                

                <Label Content="Priorité" HorizontalAlignment="Center" Margin="150,70,0,0" FontSize="18" FontWeight="Bold"/>
                <DockPanel VerticalAlignment="Center" Margin="150,0,0,160" Width="130" Height="25">
                    <Label Content="extension :"  DockPanel.Dock="Left"/>
                    <TextBox x:Name="textBoxExtensionPrio" DockPanel.Dock="Right"/>
                </DockPanel>
                <Button Content="Ajouter / Supprimer" Click="btnDeleteJob" Width="120" Height="20" Margin="150,0,0,100"/>
                <ListBox d:ItemsSource="{d:SampleData ItemCount=5}" Width="100" Height="150" Margin="150,130,0,0"/>


                <Label Content="Crypt" HorizontalAlignment="Center" Margin="500,70,0,0" FontSize="18" FontWeight="Bold"/>
                <DockPanel VerticalAlignment="Center" Margin="500,0,0,160" Width="130" Height="25">
                    <Label Content="extension :"  DockPanel.Dock="Left"/>
                    <TextBox x:Name="textBoxExtensionCrypt" DockPanel.Dock="Right"/>
                </DockPanel>
                <Button Content="Ajouter / Supprimer" Click="btnDeleteJob" Width="120" Height="20" Margin="500,0,0,100"/>
                <ListBox d:ItemsSource="{d:SampleData ItemCount=5}" Width="100" Height="150" Margin="500,130,0,0"/>

            </Grid>
        </TabItem>
        

    </TabControl>
</Window>
